"""Infrastructure layer: concrete implementations of core interfaces.

Modules in this package may import from `translator.core` ONLY.
They must NEVER import from `translator.pipeline`, `translator.ui`,
or from each other across sub-packages.
"""
