"""Core layer: interfaces, contracts, and typed message dataclasses.

This package has ZERO external dependencies. All other layers depend on core/,
but core/ depends on nothing outside the standard library + typing extensions.
"""
